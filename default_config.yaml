# a0-matrix Default Configuration
# These values are loaded when the plugin is initialized.
# Configure via Agent Zero Settings UI → Plugins → a0-matrix → ⚙️

# Matrix connection (REQUIRED)
matrix:
  homeserver_url: ""
  user_id: ""
  access_token: ""
  device_id: "A0-bot"

# Bot behavior
bot:
  display_name: "Agent Zero"
  agent_identity: "Agent Zero"
